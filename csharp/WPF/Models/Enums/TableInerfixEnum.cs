namespace WPF.Models.Enums
{
    public enum TableInerfixEnum
    {
        // AIT table prefix
        ACC,
        FLS,
        USH,
        USD,
        HSD,

        // SYS table prefix
        SGI,
    }
}
