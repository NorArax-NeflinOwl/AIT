<Window x:Class="WPF.Windows.InitWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Title="{Binding Properties.Title}" 
        Height="{Binding Properties.Heigth}" 
        Width="{Binding Properties.Width}" 
        ResizeMode="{Binding Properties.ResizeMode}" 
        Topmost="{Binding Properties.Topmost}" 
        WindowStyle="{Binding Properties.WindowStyle}"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <Image Grid.Row="0" x:Name="InitImage"/>
        <TextBlock Grid.Row="0" x:Name="InitMessage" Margin="0,0,0,15" HorizontalAlignment="Center" VerticalAlignment="Bottom"/>
        <ProgressBar Grid.Row="1" x:Name="InitProgressBar"/>
    </Grid>
</Window>
